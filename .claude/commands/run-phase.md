---
description: Kick off the next incomplete build phase under the Autonomous Execution Protocol
---

Read SPEC.md, PLAN.md, DECISIONS.md and the git log. Execute the next
incomplete phase under the Autonomous Execution Protocol (SPEC.md S0).
Do not ask me anything; S0.7 hard stops only.

Orient first with `graphify query "<question>"` / `codegraph explore` instead
of re-reading engine source into context. Delegate per the CLAUDE.md routing
table; commit after the phase gate passes (or degraded-passes are logged);
run the gate via /gate before committing.
